# 2026-08-31 번역 업데이트 검수 기록

## 배포 범위

사용자는 외부 API 없이 직접 번역·검수·지침 보강을 승인했다. 이후 제외한 나이아 장면은 원문으로 남기고 검수한 35개 스토리와 UI부터 반영하겠다는 안내에 `반영 진행`으로 승인했다.

- 신규 일반 이벤트 8개 파일과 유리·나이아의 포함 대상 캐릭터 스토리/일상 대화: 합계 35개 파일, 고유 키 1,997개, 중복 등장 포함 메시지 2,001개.
- 신규/변경 MasterData 문구 402개. 대표 구조 227개를 원문과 대조하고 수치 변형 전체를 검사했다.
- 기존 4개 스토리의 변경 원문 키 5개 및 인접 문맥을 확인했다. 기존 원문 키는 삭제하지 않고 새 정확한 키를 추가했다. 이 4개 스토리 전체를 이번에 재검수했다는 뜻은 아니다.
- 추가 수집된 런타임 UI 19개, 같은 이벤트의 혼합 클리어 조건 14개, 스킬/각성 조합 파생 키 1,872개를 보완했다.
- 단순 생성/자동 검사와 구분하여, 35개 파일 모두 초벌 후 일본어와 한국어를 처음부터 끝까지 다시 읽었다. 번역과 재검수는 같은 Codex 작업에서 수행했고 독립 검수자나 API 검수 결과로 표시하지 않는다.

## 제외한 장면

나이아 성인 에피소드 2는 성적 유아 역할극을 포함하므로 장면 전체를 번역하지 않았다. 아래 3개 파일은 한국어 파일 및 매니페스트에 넣지 않았고, 원문 그대로 둔다.

| ID | 메시지/고유 키 | 상태 |
| --- | ---: | --- |
| `hmr_11030100021` | 14 | 도입, 제외 |
| `hmr_11030100022` | 72 | 본 장면, 제외 |
| `hmr_11030100023` | 13 | 후일담, 제외 |

전체 캐시 감사 두 종류는 1,178개 ID 중 위 3개만 `missing-file`로 보고하며 exit 1이다. 검사를 수정하거나 원문 값을 넣어 통과시킨 것이 아니며, 신규 38개 전부 번역 완료로 주장하지 않는다. 비노골적인 제목·요약은 UI 번역에 포함되어 있다.

## 재검수에서 수정한 내용

- 용어집 기준으로 사령관씨, 전선기지, 페르디온, 미레스갈드, 곡갱이, 햄버그 스테이크 등 표기를 맞췄다.
- 유리 대사의 행동 주체가 흐려진 부분, 일본어식 피동문, 어색한 왕복 표현과 줄바꿈을 수정했다.
- 나이아의 숟가락 사고 결과가 빠진 문장을 복원하고, 요리하는 순서와 부부 대화의 행동 주체를 명확히 했다.
- 이벤트 7화에서 비행을 `하늘에서 달렸다`고 쓴 부분을 `하늘을 전속력으로 날아왔다`로 고쳤다.
- 기존 소피아 대사의 행동·감정 누락과 `아픈 생각` 같은 직역을 교정했다.
- `選べるSSRキャラBOX交換券`에서 교환권을 없애던 용어집·정규화 규칙을 고쳤다. 실제 BOX와 BOX 교환권을 구별하고 관련 명칭/팩 문구도 동기화했다. 재발 방지 검사 `audit-item-qualifiers.mjs`를 추가했다.
- 제안 카드 6개는 대상·스태프 수·만족도·색상 규칙을 대조했고, 기존 `ドリンク`의 `음료` 표기를 `드링크`로 고쳤다.

## 원문 이상으로 남긴 사항

- 이벤트 4화의 일부 화자 표시와 7화의 한 화자 표시는 대사 내용상 의심스럽지만 원문/게임 메타데이터를 임의로 바꾸지 않았다.
- 이벤트 7화의 `フェリさん`은 앞뒤 문맥상 펠을 가리켜 한국어에서는 펠로 처리했다.
- 같은 화의 냉동 식재료 문맥에 쓰인 `霜焼け`는 냉동 손상으로 번역했다.
- 나이아 일반 스토리 3화의 `数日後`와 뒤의 `昨日`는 원문 자체의 시간 불일치다. 원문 시간 표현을 임의로 합치지 않았다.

## 검증 및 재현

최신 MasterData 캐시: `50810988678590ec383f1f25923dba8e.dat`, 2026-08-31 19:35:17 KST. 매핑 140개는 설치 DLL의 매핑과 대조한 기존 결과를 사용했다. DLL은 변경하지 않았다.

정적 번들 생성 결과는 132개 테이블, 26,232개 엔트리, 누락 0개다. JSON·태그·일본어 잔존·변경 파일 레이아웃·스킬·각성·한계돌파·주요 UI·캐릭터 스토리 UI·장소 카드·수집 런타임 UI·아이템 교환권 검사를 실시했다. 전체 캐시 검사에서 제외한 3개 이외의 누락은 없었다.

검수 범위, 원문 인덱스 해시, 파일별 최종 SHA256, 각 자동 검사 원문 결과는 `2026-08-31-direct-review.json`에 기록한다. 레이아웃 검사 39개 파일은 신규 35개와 기존 수정 4개이며, 35~36자 watch는 허용 범위이고 36자 초과 blocking은 0이다. 신규 35개는 커밋 전 `--file <path> --fail`로도 각각 다시 검사해 전부 통과했다.

실제 게임 화면의 재진입/재생 검증은 이 기록에서 수행했다고 주장하지 않는다. 게임이 이미 켜져 있다면 새 번역을 확실히 읽도록 재시작 후 확인해야 한다. 이미지에 박힌 일본어는 이번 텍스트 번역 배포의 검증 범위가 아니다.
